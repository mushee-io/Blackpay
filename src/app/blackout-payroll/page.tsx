import { BlackoutPayroll } from "@/components/BlackoutPayroll";

export const metadata = {
  title: "Blackout Payroll — Blackpay",
  description: "Private payroll settlement on Midnight through Blackpay Protocol V2.",
};

export default function BlackoutPayrollPage() {
  return <BlackoutPayroll />;
}
